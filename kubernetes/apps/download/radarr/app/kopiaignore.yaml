---
apiVersion: v1
kind: ConfigMap
metadata:
  name: radarr-kopiaignore
data:
  .kopiaignore: |
    # Regenerable from web (TMDB/TVDB)
    MediaCover/

    # Logs and diagnostics - not needed for restore
    logs/
