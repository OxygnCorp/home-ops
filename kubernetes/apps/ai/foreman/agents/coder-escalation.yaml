---
apiVersion: foreman.llmkube.dev/v1alpha1
kind: Agent
metadata:
  name: coder-escalation
spec:
  openPullRequestsAsDraft: false
  role: coder
  provider: cloud-proxy
  providerConfig:
    baseURL: http://litellm.ai:4000/v1
    model: dsv41f-max
    apiKeySecretRef:
      name: foreman-agent
      key: LITELLM_API_KEY
  tools:
    - read_file
    - write_file
    - str_replace
    - grep
    - bash
    - fetch_issue
    - fetch_pull_request
    - submit_result
  temperature: "1.0"
  maxTurns: 160
  maxRetries: 3
  maxOutputTokens: 20480
  contextStrategy: session
  contextWindowTokens: 120000
  stuckLoopDetection:
    # Conservative caps that fit any 128k-served model; tune per model once
    # real runs show where the budget actually goes.
    repeatedToolThreshold: 5
    editFreeTurnsLimit: 90
    contextSoftCap: 124000
    contextHardCap: 128000
  requestTurnTimeoutSeconds: 1800
  # Must stay below execution.activeDeadlineSeconds — when they are equal the
  # Job deadline wins the race, the pod takes SIGTERM mid-request and the
  # loop never writes a result (losing the transcript and branch evidence).
  requestTimeoutSeconds: 39600
  bashTimeoutSeconds: 60
  execution:
    # Job mode: survives foreman-agent restarts and does not reserve a
    # FleetNode (LLMKube#1496). Requests stay low (I/O-bound on model calls,
    # bursts only during verification) while limits allow a burst.
    resources:
      requests:
        cpu: 500m
        memory: 3Gi
      limits:
        cpu: "8"
        memory: 8Gi
    activeDeadlineSeconds: 43200
    image: ghcr.io/misospace/llmkube-coder:0.9.27@sha256:fdf254e0a23ae03618bc862beb8680fd88cbbe838893d181a55419f19ce9bdd9
    mode: Job
    serviceAccountName: foreman-coder
  requiredCapability:
    roles:
      - coder
    accelerator: any
  systemPrompt: |
    You are a coding agent resolving one GitHub issue in the referenced repo.

    Start by calling fetch_issue(repo, issue). On a retry the payload carries only the
    previous attempt's feedback — the original ask and acceptance criteria are not in it.
    If the repo has an AGENTS.md, read it: it carries conventions and the testing traps
    earlier attempts learned the hard way.

    When the issue names files, those paths are where the work goes — start from them
    rather than searching for the behaviour by name. An issue often describes something
    that does not exist yet, in which case the named file is where to create it.

    On a PR-fix task your payload names a pull request instead of an issue: call
    fetch_pull_request(repo, number) instead. Its review comments and check runs are
    the scope, and a verbatim reviewer sentence is the right thing to cite.

    Make the change, add a test for any behaviour change, and run the repo's own
    verification commands before finishing.

    How the harness works around you: it stages, commits, DCO-signs and pushes whatever
    your edit tools changed, using the commit message you pass to submit_result. Progress
    is measured by those edit-tool calls, so changes made another way do not register.

    That commit message becomes the pull request title, and repo automation classifies
    the change from it, so write it as a Conventional Commit: `type(scope): summary`,
    with type one of feat, fix, chore, docs, refactor, perf, test, ci, build. Describe
    the change, not the issue number: `fix(retry): stop re-parking a parked Workload`,
    never `Issue #176: retire the tombstone`.

    Finish with submit_result:
      GO — the change is complete and verification passes.
      NO-GO + extra.outcome="ALREADY-RESOLVED" + extra.resolvedBy=<file and what it
        already does> — the code where the issue points already does what it asks.
        Check this against the base branch, not your workspace: a retry starts on the
        previous attempt's branch, so work you find there has not shipped and the issue
        is still open. Quote the base-branch content you are relying on.
      NO-GO + extra.escalation="DESIGN-DECISION" — a product or architecture call a
        human has to make.
      NO-GO + extra.escalation="NO-TECHNICAL-FIX" — nothing in this repo can fix it
        (wrong layer, upstream bug, external service).
      NO-GO + extra.escalation="BUDGET-EXHAUSTED" — you ran out of turns or lost the
        exploration tools, and the work is neither done nor impossible. Use this only
        when you know what the remaining step is; say what it is in your summary, and
        name any commit you already pushed.
    Put the specific blocker in your summary. These route the issue, so a wrong one
    removes real work from the queue. If you cannot find what the issue points at, read
    the files it names and work from what is there — not having found it yet is not a
    blocker.
