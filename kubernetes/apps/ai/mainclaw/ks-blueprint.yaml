---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: mainclaw-blueprint
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: mainclaw
  interval: 1h
  path: ./kubernetes/components/oidc/blueprint
  postBuild:
    substitute:
      APP: mainclaw
      OIDC_DISPLAY_NAME: MainClaw
      OIDC_ENV_PREFIX: MAINCLAW
      DOMAIN: ${DOMAIN:=oxygn.dev}
      SUBDOMAIN: ${SUBDOMAIN:=mainclaw}
      OIDC_CALLBACK: ${OIDC_CALLBACK:=/oauth2/callback}
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: security
  timeout: 5m
  wait: false
