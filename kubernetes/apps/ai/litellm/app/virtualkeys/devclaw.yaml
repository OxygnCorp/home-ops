---
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMVirtualKey
metadata:
  name: devclaw
spec:
  proxyRef: litellm
  keyAlias: Devclaw
  secretName: litellm-key-devclaw
  secretKey: api-key
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/pushsecret_v1alpha1.json
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: devclaw
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: onepassword
      kind: ClusterSecretStore
  selector:
    secret:
      name: litellm-key-devclaw
  data:
    - match:
        secretKey: api-key
        remoteRef:
          remoteKey: litellm-key-devclaw
          property: credential
