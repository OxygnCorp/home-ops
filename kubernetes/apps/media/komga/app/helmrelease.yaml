---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: komga
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: komga
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      komga:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: gotson/komga
              tag: "1.26.2@sha256:8bc00f642c82a7b5148c2c49289cd5f2289402072f2c9d83ccfecde5d5e55e5f"
            env:
              TZ: Europe/Paris
              SERVER_PORT: &port 80
              KOMGA_CONFIGDIR: /config
              LOGGING_FILE_NAME: /dev/stdout
            resources:
              requests:
                cpu: 15m
                memory: 640Mi
              limits:
                memory: 1024Mi
                cpu: 200m
    service:
      app:
        controller: komga
        ports:
          http:
            port: *port
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.oxygn.dev"
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    persistence:
      config:
        existingClaim: komga

      media:
        type: nfs
        server: funkstation.internal
        path: /volume2/Media/Comics
        globalMounts:
          - path: /media/comics
            readOnly: true

      books:
        type: nfs
        server: funkstation.internal
        path: /volume2/Media/Books/readarr
        globalMounts:
          - path: /media/books
            readOnly: true
