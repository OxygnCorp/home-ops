---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: teslamate
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: teslamate
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  dependsOn: []
  values:
    controllers:
      teslamate:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18@sha256:b6d3af974df781c673d37e49bdddfa14a6f5be28b18d2cb7713f0449bea4057b
              pullPolicy: Always
            env:
              INIT_POSTGRES_HOST: "{{ .Release.Name }}-rw.automation.svc.cluster.local"
              INIT_POSTGRES_USER:
                valueFrom:
                    secretKeyRef:
                      name: "{{ .Release.Name }}-app"
                      key: username
              INIT_POSTGRES_PASS:
                valueFrom:
                    secretKeyRef:
                      name: "{{ .Release.Name }}-app"
                      key: password
              INIT_POSTGRES_DBNAME:
                valueFrom:
                    secretKeyRef:
                      name: "{{ .Release.Name }}-app"
                      key: dbname
              INIT_POSTGRES_SUPER_USER:
                valueFrom:
                    secretKeyRef:
                      name: "{{ .Release.Name }}-superuser"
                      key: username
              INIT_POSTGRES_SUPER_PASS:
                valueFrom:
                    secretKeyRef:
                      name: "{{ .Release.Name }}-superuser"
                      key: password
        containers:
          app:
            image:
              repository: teslamate/teslamate
              tag: 4.1.1@sha256:cc87082e7d67a91aa11ca0fb30c2e730bc2a1fccbec9578bb3f3c64f586410f1
            env:
              TZ: Europe/Paris
              DISABLE_MQTT: "false"
              MQTT_HOST: mosquitto.automation.svc.cluster.local
              DATABASE_HOST: "{{ .Release.Name }}-rw.automation.svc.cluster.local"
              DATABASE_USER:
                valueFrom:
                    secretKeyRef:
                      name: "{{ .Release.Name }}-app"
                      key: username
              DATABASE_PASS:
                valueFrom:
                    secretKeyRef:
                      name: "{{ .Release.Name }}-app"
                      key: password
              DATABASE_NAME:
                valueFrom:
                    secretKeyRef:
                      name: "{{ .Release.Name }}-app"
                      key: dbname
            envFrom:
              - secretRef:
                  name: teslamate-secret
    service:
      app:
        controller: teslamate
        type: ClusterIP
        ports:
          http:
            port: &port 4000
    route:
      app:
        hostnames:
          - teslamate.oxygn.dev
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    resources:
      requests:
        cpu: 25m
        memory: 256Mi
      limits:
        memory: 384Mi
        cpu: 200m
