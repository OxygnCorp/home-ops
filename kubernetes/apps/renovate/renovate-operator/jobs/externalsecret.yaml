---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mushu-bot-pem
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: mushu-bot-pem
  data:
    - secretKey: private_key_pem
      remoteRef:
        key: github
        property: GITHUB_APP_PRIVATE_KEY
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
# The generator produces GitHub App installation tokens that expire exactly
# 1h after issuance. Pods resolve envFrom at container start, so the token
# baked into a run's env is at most (refreshInterval) old - keep it well
# below the 1h lifetime so runs never boot with an expired token.
metadata:
  name: &name mushu-bot-auth-token
spec:
  refreshInterval: 10m
  target:
    name: *name
    template:
      type: Opaque
      engineVersion: v2
      data:
        GITHUB_COM_TOKEN: "{{ .token }}"
        GITHUB_COM_USER: b3h5Z24tYWRtaW4=
        RENOVATE_TOKEN: "{{ .token }}"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: GithubAccessToken
          name: mushu-bot-auth-token
